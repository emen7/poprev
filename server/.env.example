# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/poprev
# For production, use your MongoDB Atlas connection string or similar service
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/poprev

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=30d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
